import { Callout } from "./Callout";
import { ItemCard } from "./ItemCard";

/** Components made available to MDX content (passed to compileMDX). */
export const mdxComponents = { Callout, ItemCard };
